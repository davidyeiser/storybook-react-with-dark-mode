import React from 'react'
import Label from './'

export default {
  title: 'Label'
}

export const Basic = () => (
  <Label>Email</Label>
)

export const Disabled = () => (
  <Label disabled>Email</Label>
)
